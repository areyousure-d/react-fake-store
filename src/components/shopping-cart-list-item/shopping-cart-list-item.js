import React from 'react';
import PropTypes from 'prop-types';

import { fetchProductById } from '../../redux/actions';

const ShoppingCartListItem = ({ id }) => {

  return (
    <div>
      ShoppingCartListItem
    </div>
  );
};

export default ShoppingCartListItem;

